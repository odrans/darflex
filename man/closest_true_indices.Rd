% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.r
\name{closest_true_indices}
\alias{closest_true_indices}
\title{Find the Closest Previous True Value Index}
\usage{
closest_true_indices(logical_vector)
}
\arguments{
\item{logical_vector}{A logical vector where \code{TRUE} and \code{FALSE} values are used to identify positions of interest. \code{NA} values are ignored.}
}
\value{
A numeric vector of the same length as \code{logical_vector}. Each element corresponds to the index of the closest previous \code{TRUE} value, or \code{NA} if none exists.
}
\description{
This function returns the index of the closest previous \code{TRUE} value in a logical vector for each element. If an element is \code{TRUE}, it returns its own index. If no \code{TRUE} value is found before a given position, it returns \code{NA}.
}
\details{
The function scans through the input logical vector and identifies the closest previous index where a \code{TRUE} value occurs. If the element itself is \code{TRUE}, its index is returned.
}
